{
  "// NOTE": "Dummy config for the test project only. No secrets. Config.cs (Agrumy.Shared) reads appsettings.json from the working directory at static-init time, so this file must exist next to the test assembly.",
  "ConnectionStrings": {
    "DefaultConnection": "server=localhost;database=agrumy_tests_unused;user id=test;password=test;"
  },
  "JWT": {
    "SecureKey": "unit-test-signing-key-not-a-secret-0123456789ABCDEF",
    "Issuer": "https://tests.agrumy.local",
    "Audience": "agrumy-api-tests"
  },
  "WebView": {
    "ApiService": "http://localhost:5000"
  }
}
